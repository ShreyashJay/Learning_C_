# include <stdio.h>
# include <stdlib.h>  // rand is one of the constituents,don't know much
# include <time.h>    // for time,don't know much
# include <math.h>

int main()
{

 /*    string with spaces
    char name[100];
    scanf("%[^\n]s",name);
    printf("The string is: %s ",name);    */


    /*  generate random int between two numbers
int a,b,c,r;
srand(time(NULL));
for(b=0;b<5;b++){   r=rand()%46+5 ;   printf("%d \n",r);};
// rand() generates random number from 0 to some upper limit
// rand()%6 generates random number from 0 to 5
// rand()%6+4 generates random number from (0+4) to (5+4)
*/


return 0;}
